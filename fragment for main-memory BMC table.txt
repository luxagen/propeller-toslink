{
' turn the low 8 bits of the frame into an effective address for the corresponding table entry
mov temp,frame
and temp,#$0FF
shl temp,1
and temp,#$0FF
add temp,bmc_table
' replace the entry's address with its value
rdword temp,temp

' worst case 44 cycles
}

